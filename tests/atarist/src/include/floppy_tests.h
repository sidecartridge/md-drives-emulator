#ifndef FLOPPY_TESTS_H
#define FLOPPY_TESTS_H

/* The floppy tests, run by FLOPTEST against the disk in A: built by
   tools/dev/make_floppy_image.py. */
int run_floppy_tests(void);

#endif  // FLOPPY_TESTS_H
